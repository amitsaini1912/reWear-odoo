<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= item.title %> | reWear</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .item-image {
      max-height: 400px;
      object-fit: cover;
      border-radius: 8px;
    }
    .tag-badge {
      background-color: #e0f0ff;
      color: #007bff;
      margin-right: 5px;
      font-size: 0.85rem;
    }
    .item-meta {
      font-size: 0.95rem;
      color: #555;
    }
  </style>
</head>
<body class="bg-light">

  <div class="container mt-5">
    <a href="/items" class="btn btn-outline-secondary mb-4">← Back to Items</a>

    <div class="card shadow-sm">
      <div class="row g-0">
        <div class="col-md-5">
          <img src="/uploads/<%= item.image %>" alt="<%= item.title %>" class="img-fluid item-image w-100">
        </div>
        <div class="col-md-7">
          <div class="card-body">
            <h2 class="card-title"><%= item.title %></h2>
            <p class="card-text text-muted"><%= item.description %></p>

            <div class="mb-3">
              <% if (item.tags && item.tags.length) { %>
                <% item.tags.forEach(tag => { %>
                  <span class="badge tag-badge"><%= tag %></span>
                <% }) %>
              <% } %>
            </div>

            <ul class="list-group list-group-flush mb-3">
              <li class="list-group-item"><strong>Category:</strong> <%= item.category %></li>
              <li class="list-group-item"><strong>Type:</strong> <%= item.type %></li>
              <li class="list-group-item"><strong>Size:</strong> <%= item.size %></li>
              <li class="list-group-item"><strong>Condition:</strong> <%= item.condition %></li>
            </ul>

            <p class="item-meta mt-3">
              <strong>Uploaded by:</strong> <%= item.uploader?.username || 'Unknown User' %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
